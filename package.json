{
  "private": true,
  "workspaces": [
    "api",
    "emailer",
    "ui"
  ],
  "scripts": {
    "precommit": "pretty-quick --staged && yarn test:api && yarn test:emailer && yarn test:ui",
    "test:api": "pushd api && yarn test:ci && popd",
    "test:emailer": "pushd emailer && yarn test && popd",
    "test:ui": "pushd ui && CI=true yarn test && popd"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "prettier": "^1.13.6",
    "pretty-quick": "^1.6.0"
  }
}
