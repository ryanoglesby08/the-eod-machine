{
  "private": true,
  "workspaces": [
    "api",
    "emailer",
    "ui"
  ],
  "scripts": {
    "precommit": "pretty-quick --staged && yarn test:api",
    "test:api": "pushd api && yarn test && popd"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "prettier": "^1.13.6",
    "pretty-quick": "^1.6.0"
  }
}
