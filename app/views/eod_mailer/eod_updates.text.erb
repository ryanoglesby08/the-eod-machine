EOD Updates from <%= @from_team_location.name %> for <%= today_long(ActiveSupport::TimeZone.new(@from_team_location.time_zone)) %>

<% @categories.each do |category| %>
    ---------------------------------------------------------------------
    <%= category.name %>

    <% category.entries.each do |entry| %>
      * <%= entry_with_author(entry) %>
    <% end %>
<% end %>

- Delivered by The EOD Machine